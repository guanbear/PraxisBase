# Tasks

- [x] Parse per-knowledge-base profiles, modes, and filter rules from `.praxisbase/config.yaml`.
- [x] Apply per-KB built-in filter rules before wiki curation.
- [x] Keep existing local `.praxisbase/filter-rules.yaml` behavior compatible.
- [x] Render visible per-KB rule summaries and emit `dist/knowledge-config.json`.
- [x] Update the local OpenClaw/K8s project config.
- [x] Add tests for parsing, filtering, and site rendering.
- [x] Rebuild TypeScript and run focused tests.
