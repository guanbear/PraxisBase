# Tasks

- [x] Add core `auditKb` and `pruneKb` with tests.
- [x] Add CLI `kb` command with audit/prune tests.
- [x] Add rebuild wrapper that delegates to daily experience flow.
- [x] Run focused tests, `pnpm check`, and non-destructive local smoke.
