# M25 Real Smoke Audit - 2026-05-30

This audit records the bounded real personal smoke for M25 Memory-First Experience Distillation. It intentionally references report paths and aggregate findings only; private runtime evidence under `.praxisbase/` and generated site files are not committed.

## Command

```bash
node packages/cli/dist/index.js daily run --mode personal --degraded --no-ai --max-curation-proposals 4 --skill-synthesis --max-skill-candidates 2 --build-site --progress --json
```

## Reports Reviewed

- Daily report: `.praxisbase/reports/daily/daily-experience_2026-05-30t06-43-43-138z.json`
- Lesson report: `.praxisbase/reports/lessons/lesson_2026-05-30t06-43-43-138z.json`
- Wiki curation report: `.praxisbase/reports/wiki-curation/wiki-curation-report_2026-05-30t06-43-43-138z.json`
- Skill synthesis report: `.praxisbase/reports/skill-synthesis/skill-synthesis_20260530064343.json`
- Generated site: `dist/index.html`, `dist/review.html`, `dist/pages/*.html`, `dist/graph.json`

## Smoke Findings

- The run was intentionally degraded and did not call the LLM lesson extractor: `ai_distill.mode=disabled`, `lessons.ai_lessons=0`, and `ai_distill.warnings` includes `ai_distill_disabled`.
- Source coverage was real and broad: the lesson report selected 329 spans from 871 source items, including local OpenClaw, Codex, and trusted remote OpenClaw staged material.
- The lesson layer produced 22 deterministic lessons before dedupe/state routing. The final visible states in the daily report were `active_personal=1`, `wiki_ready=0`, `skill_ready=2`, `human_required=0`, and `rejected=0`.
- The useful visible lessons were:
  - private-route remote access as `active_personal`;
  - Slack raw user id boundary/redaction as `skill_ready`;
  - ACK before slow tool/network/dispatch work as `skill_ready`.
- Wiki proposal candidates were generated from lesson evidence and include M25 sections for applicability, negative cases, agent use, related pages, and span provenance.
- The generated HTML site exposes M25 lesson counts, top lesson summaries, pending wiki candidates, pending skill candidates, review reasons, and graph links. `dist/graph.json` contains 8 nodes, 29 links, and no broken links/orphans/duplicates.
- Stable PB pages are visible in graph/site source ids through `stable_kb:*` entries. GBrain and AgentMemory sidecars were not promotion evidence in this run; the authority contract explicitly ranks them below stable PB pages, promoted skills, and active personal lessons.

## Quality Gaps Before Full Daily

- This smoke does not prove production LLM lesson extraction quality because it used `--no-ai`. A follow-up AI smoke must run with a finite uncached budget and verify cache reuse.
- `wiki_ready=0` in this run. The wiki candidates were built from `skill_ready` lesson evidence and stable pages, not from a final wiki-ready lesson state.
- AgentMemory health failed with `agentmemory_health_failed: fetch failed`, so AgentMemory sidecar import/export behavior was not exercised.
- GBrain sidecar/export behavior was not exercised because there were no changed stable PB pages and no publish path was requested.
- Skill candidates still require human review. One candidate had `skill_shape_invalid:malformed_procedure_heading`; another had semantic review `revise` with a truncated procedure sentence.
- The review queue remains large because privacy triage reported 58 human-required exceptions. This is expected for private runtime material, but it blocks some potentially useful evidence until triaged.
- The current span provenance is useful for audit, but review surfaces must keep raw private excerpts hidden by default and should avoid making noisy candidate fragments the first user-facing content.

## Outcome

M25 real smoke is acceptable as a bounded degraded smoke: it proves memory-first ingestion, deterministic lesson routing, wiki/skill candidate generation, HTML visibility, privacy gating, and authority ranking. It is not sufficient as the final production quality proof. The next validation should be a small AI-enabled daily run with cached GLM extraction and sidecar health checks.

## AI Follow-Up

Follow-up command:

```bash
node packages/cli/dist/index.js daily run --mode personal --max-ai-chunks 3 --ai-concurrency 2 --ai-timeout-ms 45000 --max-curation-proposals 3 --skill-synthesis --max-skill-candidates 2 --build-site --progress --json
```

Findings:

- GLM-4.7 production AI distill completed: `chunks=8`, `distilled=8`, `failed=0`, `cache_hits=5`, `budget_used_uncached=3`.
- The finite budget correctly skipped hidden M25 lesson LLM calls and reported `lesson_ai_skipped_by_finite_budget`.
- Context juice saved 48119 bytes from 147493 input bytes in the daily run.
- Trusted remote OpenClaw was fetched and staged, but 17 remote items still require privacy triage.
- AgentMemory remained unavailable (`agentmemory_health_failed: fetch failed`), and GBrain doctor was unhealthy before the run, so sidecar behavior still needs separate repair.
- A direct bounded `lesson extract --ai` against local OpenClaw memory verified the M25 LLM path and cache reuse. The first run wrote one lesson-extract cache entry; the second identical run reported `hits=1`, `misses=0`, `writes=0`.
- The direct AI extraction also exposed a quality guard bug: generic placeholder lessons such as "Reusable agent lesson" could be accepted. The extractor now rejects those placeholders before cache/write/state routing.

## Quality Gate Follow-Up

Additional validation found that deterministic M25 extraction and the lesson-state cache could still preserve raw OpenClaw candidate-corpus noise. The bad signature looked like repeated `Candidate:` transcript fragments with `confidence: 0.00`, `.dreams/session-corpus`, staged candidate status, or untrusted conversation metadata. This is not a reusable lesson and must not become active personal context, skill input, or wiki evidence.

Fixes:

- Deterministic lesson extraction now skips raw candidate-corpus spans before pattern matching.
- Lesson-state cache loading/upsert now drops stale raw candidate-corpus records so previously cached bad lessons cannot keep resurfacing.
- Daily summary now carries the lesson authority contract from the lesson report; when lesson-derived wiki evidence exists, the daily report exposes `wiki_semantic_input=lesson_clusters`.

Verification:

- Focused tests passed for deterministic extraction, lesson cache cleanup, and M25 production integration.
- A real local OpenClaw `lesson extract --max-spans 80` no longer returns the old `Candidate: route=direct ... confidence: 0.00` active lesson.
- A bounded no-AI daily run produced `deterministic_lessons=12`, `skill_ready=1`, `wiki_evidence=1`, and `authority_contract.wiki_semantic_input=lesson_clusters` without spending GLM tokens.

Remaining gaps:

- AgentMemory is configured but the local daemon is not installed/running (`agentmemory not found`, daemon fetch failed).
- Trusted remote OpenClaw still produces privacy-required records on each daily run; safe abstraction and triage reuse need another pass before remote memory can flow smoothly into stable wiki/skill outputs.
- Many Codex/OpenClaw session files are oversize and currently enter only as bounded samples; better session pre-summarization/selection is still needed for full personal coverage without large token spend.

## M25.1 Personal GA Follow-Up - 2026-05-31

Golden validation now covers the four personal GA source families used for acceptance:

- `openclaw-local`: 8 / 8 targets matched, 0 private leaks.
- `openclaw-remote`: 8 / 8 targets matched, 0 private leaks.
- `codex-app`: 5 / 5 targets matched, 0 private leaks.
- `codex-cliproxyapi`: 5 / 5 targets matched, 0 private leaks.

Commands:

```bash
node packages/cli/dist/index.js lesson golden --json
pnpm test -- tests/core/m25-golden-validation.test.ts
```

The Codex fixtures exercise explicit user preference, verified fix, target-machine confirmation, generated-directory veto, ACK before slow work, repeated failure, tool sequence, and provider-secret handling decisions. Each matched lesson includes span provenance.

Current real degraded personal daily command:

```bash
node packages/cli/dist/index.js daily run --mode personal --degraded --no-ai --max-curation-proposals 3 --skill-synthesis --max-skill-candidates 2 --build-site --progress --json
```

Reports reviewed:

- Daily report: `.praxisbase/reports/daily/daily-experience_2026-05-31t03-15-10-170z.json`
- Lesson report: `.praxisbase/reports/lessons/lesson_2026-05-31t03-15-10-170z.json`
- Wiki curation report: `.praxisbase/reports/wiki-curation/wiki-curation-report_2026-05-31t03-15-10-170z.json`
- Skill synthesis report: `.praxisbase/reports/skill-synthesis/skill-synthesis_20260531031510.json`
- Generated site: `dist/index.html`, `dist/review.html`

Findings:

- The run did not spend LLM tokens: `ai_distill.mode=disabled`, with configured model metadata `GLM-4.7`.
- The configured personal sources were read: Codex app archived sessions, Codex app sessions, codex-cliproxyapi sessions, trusted remote OpenClaw, local OpenClaw memory, local OpenClaw reports, and AgentMemory sidecar.
- Source coverage in `personal_ga` shows Codex sessions available with 4635 items and 60 content spans, and OpenClaw memory available with 936 items and 41 content spans.
- The lesson layer produced `deterministic_lessons=30`, `active_personal=1`, `wiki_ready=8`, `skill_ready=3`, `human_required=0`, and `wiki_evidence=11`.
- Every visible lesson has a disposition: `lesson_count=13`, `disposition_count=13`.
- The wiki path used lesson clusters as promotion evidence: `authority_contract.wiki_semantic_input=lesson_clusters`; sidecars are still not promotion evidence.
- The run generated 2 wiki proposal candidates, 2 skill candidates needing human review, and 8 site pages.
- The generated Personal GA report correctly says this is not production-ready because the run was degraded, AgentMemory was unavailable, and privacy triage still has blockers.

Remaining blockers:

- `personal_ga.production_ready=false` with blockers `ai_lesson_extraction_disabled`, `source_unavailable:agentmemory:sidecar_import`, and `source_blocked:agentmemory:sidecar_import`.
- Latest next action reports 72 items needing privacy triage before they can become wiki evidence.
- This follow-up intentionally did not run a new GLM AI daily smoke. The previous bounded AI follow-up remains the token/cache proof; the current run is the deterministic real-source regression proof after M25.1 stabilization.

## Remote Noise Follow-Up

Additional no-AI validation showed that trusted remote OpenClaw `latest.json` can already contain `source_ref` values under `memory/dreaming/` and raw `Candidate:` corpus fragments. The earlier SQLite query filter was not enough because those JSON export items entered `resolveExperienceSource` before lesson extraction or privacy triage.

Fixes:

- The OpenClaw source adapter now excludes `memory/dreaming/`, `.dreams`, and `dream-diary` items before envelope creation and privacy triage.
- The local OpenClaw SQLite adapter uses the same path exclusions as source inventory, so dream rows do not become source envelopes.
- Candidate-corpus filtering checks each parsed item first; one noisy JSON item no longer causes every item in the same export payload to be skipped.

Verification:

- Source-adapter tests cover local SQLite dream chunks and trusted remote OpenClaw exported JSON dream items.
- `pnpm test -- tests/core/experience-source-adapters.test.ts` passed.
- A bounded real no-AI daily run completed without GLM calls. Trusted remote OpenClaw changed from the prior `fetched=20, human_required=17` shape to `fetched=16, human_required=12`, and the overall privacy queue dropped from 50 to 45.

Remaining gaps:

- The remaining trusted remote blockers are mostly concrete host/path/account/private operational details. They should be abstracted or triaged, not auto-released by the dreaming filter.
- Codex and codex-cliproxyapi still have many oversize sessions; M25 needs the later pre-summary/session-selection pass to improve useful Codex lesson coverage.

## M25.2 Personal GA Contract Follow-Up - 2026-05-31

Follow-up command:

```bash
node packages/cli/dist/index.js daily run --mode personal --max-ai-chunks 8 --ai-concurrency 8 --ai-timeout-ms 45000 --max-curation-proposals 3 --skill-synthesis --max-skill-candidates 2 --build-site --progress --json
```

Reports reviewed:

- Daily report: `.praxisbase/reports/daily/daily-experience_2026-05-31t14-03-02-085z.json`
- Lesson report: `.praxisbase/reports/lessons/lesson_2026-05-31t14-03-02-085z.json`
- Generated site: `dist/index.html`, `dist/review.html`

Findings:

- GLM-4.7 production AI distill completed with `chunks=26`, `distilled=26`, `failed=0`, `cache_hits=19`, `budget_max_uncached=8`, `budget_used_uncached=7`, and `skipped_by_budget=0`.
- Personal GA reported `mode=production_ai`, `production_ready=true`, and no blocking reasons.
- The finite-budget lesson warning remained informational only. It means the extra lesson-pipeline LLM pass was skipped under a bounded smoke budget; source AI distill was not exhausted.
- AgentMemory sidecar remained unavailable and was correctly downgraded to `optional_sidecar_unavailable:agentmemory:sidecar_import` because PB produced usable core output.
- PB core output was usable without sidecars: `active_personal=1`, `wiki_ready=8`, `skill_ready=3`, and `wiki_evidence=11`.
- Agent consumption was verified through PB context:

```bash
node packages/cli/dist/index.js context get --agent openclaw --stage diagnosis --query "OpenClaw Codex lessons" --mode personal --json
node packages/cli/dist/index.js context get --agent codex --stage diagnosis --query "OpenClaw Codex lessons" --mode personal --json
```

Both commands returned stable PB context items such as OpenClaw dispatch routing failures, ACK timing before long-running agent work, OpenClaw Slack replay and post-deploy stability failures, gateway restart after configuration changes, and task runner presence checks.

Outcome:

M25.2 changes the personal GA gate from "all integrations perfect" to "PB core output usable by agents, with hard privacy failures still blocking." Optional sidecar outages and bounded proposal queues no longer make a successful personal run look unusable, while rejected private material, missing PB context, no promoted/active PB knowledge, disabled AI extraction, and true uncached budget exhaustion still block release readiness.

## Latest Lesson Curation Follow-Up

Follow-up command:

```bash
node packages/cli/dist/index.js daily run --mode personal --degraded --no-ai --build-site --progress --json
```

Reports reviewed:

- Daily report: `.praxisbase/reports/daily/daily-experience_2026-05-30t09-05-08-433z.json`
- Lesson report: `.praxisbase/reports/lessons/lesson_2026-05-30t09-05-08-433z.json`
- Wiki curation report: `.praxisbase/reports/wiki-curation/wiki-curation-report_2026-05-30t09-05-08-433z.json`

Findings:

- The run stayed bounded and did not spend LLM tokens: `ai_distill.mode=disabled`.
- M25 memory-first extraction produced `deterministic_lessons=30`, `active_personal=1`, `wiki_ready=8`, `skill_ready=3`, `wiki_evidence=11`, and `proposal_candidates=3`.
- The source privacy queue still reported 45 human-required items across raw source ingestion, while the latest lesson report had no lesson-level human-required privacy records.
- The deterministic extractor now preserves high-value Chinese and mixed-language OpenClaw lessons under noisy equal-score memory spans, including self-test after changes, target-machine confirmation, NOCASE collation, browser cache busting, failover routing, voice delivery, Slack raw user boundary/redaction, and ACK before slow work.
- Wiki curation now reads only the latest M25 lesson report, so stale historical lesson reports cannot pollute new candidates.
- Distinct lessons from the same memory span are preserved by lesson signature/id instead of being collapsed by `source_ref:source_hash`.
- Candidate page span provenance now lists source, span, line, and heading only. It no longer embeds long raw excerpts in review pages.
- Personal mode can use abstracted `personal_only` lessons as wiki evidence; team mode still excludes them.
- Leak checks over current wiki proposals and generated site found no raw candidate corpus markers or private machine/account/path strings such as `Candidate:`, `session-corpus`, raw Slack user ids, `root@`, the private remote host, private SSH alias, or local user paths.

Remaining gaps:

- Curation still writes only 3 proposal files because the default proposal limit is 3. Additional ready lessons appear as lesson candidates but are not all materialized as proposal pages yet.
- Codex and codex-cliproxyapi useful lesson coverage remains weak because many sessions are oversize and need better pre-summary/selection.
- AgentMemory sidecar remains unavailable in local real runs (`agentmemory_health_failed: fetch failed`), so interop behavior is not proven by this smoke.
